[package]
name = "candles_rs"
version = "0.0.0"
edition = "2021"
authors = ["Kael Zhang <i+pypi@kael.me>"]
description = "High-performance Rust core for the candles time-series kernel"
license = "MIT"
homepage = "https://github.com/kaelzhang/candles"
repository = "https://github.com/kaelzhang/candles"

[lib]
name = "candles_rs"
crate-type = ["cdylib"]

[dependencies]
pyo3 = { version = "0.25", features = ["extension-module"] }

[features]
default = []
# Enable this feature to skip building the extension module
extension-module = ["pyo3/extension-module"]

[profile.release]
lto = true
codegen-units = 1
opt-level = 3
